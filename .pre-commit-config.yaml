repos:
  - repo: local
    hooks:
      - id: pytest-unit
        name: Run unit tests
        entry: python -m pytest tests/unit/ --tb=short -q
        language: system
        pass_filenames: false
        always_run: true
